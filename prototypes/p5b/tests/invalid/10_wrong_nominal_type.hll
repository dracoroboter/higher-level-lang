// Test: passare String dove serve un tipo nominale
// MUST BE REJECTED: "hello" is String, not Email

type Email = String where matches("^.+@.+$")

function send(Email to) {
    printLn(to)
}

function main() {
    send("not-an-email")  // ERROR: String is not Email
}
